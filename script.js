let cart=[];function addToCart(productName,price){cart.push({name:productName,price:price});updateCartCount();alert(productName+" has been added to your cart!")}function updateCartCount(){document.getElementById("cart-count").textContent=cart.length}function showCart(){if(cart.length===0){alert("Your CENT cart is empty.");return}let message="YOUR CENT CART\n\n";let total=0;cart.forEach((item,index)=>{message+=(index+1)+". "+item.name+" - GH₵ "+item.price+"\n";total+=item.price});message+="\nTotal: GH₵ "+total;alert(message)}function filterProducts(category){document.querySelectorAll(".product-card").forEach(product=>{product.style.display=category==="all"||product.getAttribute("data-category")===category?"block":"none"})}function searchProducts(){const searchText=document.getElementById("search").value.toLowerCase();document.querySelectorAll(".product-card").forEach(product=>{const name=product.querySelector("h3").textContent.toLowerCase();product.style.display=name.includes(searchText)?"block":"none"})}function contactMessage(){alert("Thanks for contacting CENT!\n\nContact features will be added soon.")}updateCartCount();
function contactFormMessage(event){event.preventDefault();alert("Thanks for contacting CENT! Your message has been received.");}
